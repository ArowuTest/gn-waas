// GN-WAAS Flutter — Default widget test (smoke test)
// This file is intentionally minimal. See test/screens/ for full widget tests.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, 2);
  });
}
