# NOTE: In production, use Kubernetes Secrets with external-secrets-operator
# or HashiCorp Vault. Never commit real secrets to git.
# This file shows the structure only — values are placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: gnwaas-secrets
  namespace: gnwaas
type: Opaque
stringData:
  DB_USER: "gnwaas_user"
  DB_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_REDIS_PASSWORD"
  MINIO_ACCESS_KEY: "REPLACE_WITH_MINIO_KEY"
  MINIO_SECRET_KEY: "REPLACE_WITH_MINIO_SECRET"
  GRA_API_KEY: "REPLACE_WITH_GRA_API_KEY"
  KEYCLOAK_CLIENT_SECRET: "REPLACE_WITH_KEYCLOAK_SECRET"
